/*******************************************************/
/**************Código para hacer una tabla**************/
/*******************************************************/

//Métodos para implementar en el .h//

#include <QVector>
#include <QDialog>
#include <QHBoxLayout>
#include <QAbstractTableModel>
#include <QModelIndex>
#include <QTimer>

class ModeloTabla : public QAbstractTableModel {

public:
	ModeloTabla(QVector<Bola*> *);
	QVector<Bola*> *pBolas;

	int	rowCount(const QModelIndex &parent = QModelIndex()) const;

	int	columnCount(const QModelIndex &parent = QModelIndex()) const;

	QVariant headerData(int section, Qt::Orientation orientation, int role = Qt::DisplayRole) const;

	QVariant data(const QModelIndex &index, int role = Qt::DisplayRole) const;

	bool setData(const QModelIndex &index, const QVariant &value, int role = Qt::EditRole);

	Qt::ItemFlags flags(const QModelIndex &index) const;

	void update();

};

//Métodos para implementar en el .cpp//

int ModeloTabla::rowCount(const QModelIndex &parent) const {
}

int ModeloTabla::columnCount(const QModelIndex &parent) const {
}

QVariant ModeloTabla::headerData(int section, Qt::Orientation orientation, int role) const {
}


/*******************************************************/
/*******************************************************/
